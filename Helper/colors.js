export default function getColors() {
    return {
        primary: '#3E3364',     // Deep purple (for header and bottom tab)
        secondary: '#4F4C75',   // Slightly muted purple (for background gradient)
        accent: '#FFD700',      // Bright yellow (for icon highlight on active state)
        white: '#FFFFFF',       // Default white
        black: '#000000',       // Default black
        background: '#A49FC4',  // Light lavender (for background)
        Button: '#0000FF',      // Default Blue 
        gray: '#D3D3D3',        // Default gray for unselected tab icons
        text: '#E5E5E5',        // Light white (for text inside the dark areas)
        buttonText: '#FFFFFF',  // Default white (for button text)
        cancel: '#FF0000',      // Default red (for cancel button)
      };
}